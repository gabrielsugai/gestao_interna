<h2>Bots</h2>
<% if @bots.empty?%>
  <p>Ainda não foi criado nenhum bot!</p>
<% end %>
<div>
  <% @bots.each do |bot|%>
  <ul>
    <%= bot.company.name %>
    <%= bot.order.plan.name %>
    <%= Bot.human_attribute_name("status.#{bot.status}") %>
    <%= bot.token %>
  </ul>
<% end %>
</div>